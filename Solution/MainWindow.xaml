<Window x:Class="Solution.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Solution" 
        xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Height="720" Width="1250" Title="一种刨槽图像化编程解决方案demo"
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Margin="10" Background="White" x:Name="Grdrawing" MouseLeftButtonUp="Grid_MouseLeftButtonUp">
            <!--<Behaviors:Interaction.Triggers>
                <Behaviors:EventTrigger EventName="MouseLeftButtonUp">
                    <Behaviors:InvokeCommandAction Command="{Binding Painting}"
                                                   CommandParameter="{Binding ElementName=Grdrawing}"/>
                </Behaviors:EventTrigger>
            </Behaviors:Interaction.Triggers>-->
        </Grid>

        <Grid Grid.Column="1">
            
        </Grid>
        
    </Grid>
</Window>
